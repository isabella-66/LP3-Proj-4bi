@model IEnumerable<LP3_ProjetoFinal.Models.CleaningTime>

@{
    ViewData["Title"] = "Horários de limpeza";
}
<h1>@ViewData["Title"]</h1>

@if(Model.Count() == 0)
{
    <p>Nenhum horário cadastrado</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Horário de início</th>
                <th>Horário de término</th>
                <th>Funcionário</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var hour in Model)
            {
                <tr>
                    <td>@hour.Id</td>
                    <td>@hour.StartTime</td>
                    <td>@hour.EndTime</td> 
                    <td>@hour.Employee</td>
                    <td>
                       <a class="btn btn-primary btn-sm" href="/CleaningTime/Update/@hour.Id">Editar</a>
                       <a class="btn btn-primary btn-sm" href="/CleaningTime/Delete/@hour.Id">Remover</a> 
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
